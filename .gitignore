# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Secrets / credentials — never commit these
*.env
.env*
appsettings.local.json
secrets.json

# Node (if any tooling is added later)
node_modules/
npm-debug.log*

# Python (if any tooling is added later)
__pycache__/
*.py[cod]
.venv/

# Build / temp
dist/
*.tmp
*.log

# Demo tool — generated screenshots (debug output and run captures)
tools/debug-screenshot.png
tools/result-screenshot.png
tools/screenshots/
tools/step*.png

# Local demo tool config — not committed so each machine can tune its own settings
tools/demo.config.json
